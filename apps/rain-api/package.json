{
  "name": "rain-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn rain_backend.main:app --reload --port 8000",
    "build": "echo 'No build step required for FastAPI'",
    "lint": "uv run ruff check ."
  }
}
