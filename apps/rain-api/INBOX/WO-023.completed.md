# WO-023: Completed
**Summary:** Implemented the feedback API and reasoning extraction logic in the chat orchestrator.

**Actions taken:**
- Added `PUT /v1/messages/{id}/feedback` in `backend/src/rain_backend/api/v1/messages.py`.
- Updated `run_chat` in `backend/src/rain_backend/orchestrator/chat_mode.py` with streaming `<think>` tag detection.
- Added `reasoning` chunk type to SSE stream (`yield ChatChunk(type="reasoning", data=token)`).
- Updated `Message` Pydantic model and `MessageService` to support `reasoning_content` and `feedback`.
- Verified that reasoning content is stripped from the main message before persistence and stored in its own column.

**Next Steps:**
- Frontend Agent to implement UI components for Feedback, Thinking, Edit, and Copy in WO-024.
