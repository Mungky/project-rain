# WO-027: Completed
**Summary:** Implemented hosted provider adapters for Anthropic, OpenAI, and Google Gemini, enabling access to frontier models.

**Actions taken:**
- Added `anthropic`, `openai`, and `google-generativeai` to `pyproject.toml` and synchronized the environment.
- Created adapter implementations:
    - `backend/src/rain_backend/providers/anthropic.py`
    - `backend/src/rain_backend/providers/openai.py`
    - `backend/src/rain_backend/providers/google.py`
- Updated `backend/src/rain_backend/providers/__init__.py` to export and initialize new providers based on API keys.
- Implemented `GET /v1/models` in `backend/src/rain_backend/api/v1/models.py`.
- Registered the models router in `backend/src/rain_backend/main.py`.
- Verified that all adapters conform to the `Provider` protocol and handle streaming correctly.

**Next Steps:**
- Frontend can now use `GET /v1/models` to allow users to switch models in the UI.
- Proceed to Skill Ecosystem (WO-028).
