# WO-025: Completed
**Summary:** Implemented the Document List and Delete API, enabling full management of uploaded files across all data stores.

**Actions taken:**
- Updated `backend/src/rain_backend/schemas/document.py` with `DocumentListResponse`.
- Added `list_documents` and `delete_document` to `DocumentService`.
- Implemented `GET /v1/documents` and `DELETE /v1/documents/{document_id}` in `api/v1/documents.py`.
- Verified multi-store deletion: removing a document now cleans up Postgres (row), MinIO (object), and Qdrant (vectors).
- Ensured proper indentation and idiomatic FastAPI patterns.

**Next Steps:**
- Frontend Agent to implement the Document Management UI in WO-026.
