# WO-040: Completed
**Summary:** Implemented dynamic skill filtering, user preference injection, and Neural Context sync.

**Actions taken:**
- Added logic in `chat_mode.py` to query `UserPreference` and prepend `custom_system_prompt` and `user_context` to the base instruction.
- Added skill filtering logic in `run_chat` based on `conversation.auto_skills` and `conversation.enabled_skills`.
- Added `yield ChatChunk(type="neural_context", ...)` to push live RAG snippets to the frontend.
- Added `PATCH /v1/conversations/{id}` to allow toggling skills and updating conversation metadata.
- Verified that `GET /v1/skills` exists and returns the list of available tools.
- Updated `CHANGELOG.md`.

**Next Steps:**
- Frontend Agent needs to build the UI for these features in WO-041.
