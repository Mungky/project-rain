# WO-018: Completed
**Summary:** Created SQLAlchemy models for Documents and Skills, and applied migrations to the database.

**Actions taken:**
- Created `db/schemas/document.py` with `Document` and `DocumentStatus`.
- Created `db/schemas/skill.py` with `Skill` and `SkillExecution`.
- Updated `db/schemas/__init__.py` to export new models.
- Generated and applied Alembic migration `3c737a5a064d_documents_and_skills`.
- Verified that all new columns use `TIMESTAMPTZ` and have proper SQL comments.
- Verified migration head is at `3c737a5a064d`.

**Next Steps:**
- Backend Agent should implement the upload pipeline in WO-019.
