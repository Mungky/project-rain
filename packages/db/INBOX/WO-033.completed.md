# WO-033: Move Model column to Conversation table — COMPLETED

**Revision:** 3d8803d16ffc
**Date:** 2026-04-23
**Status:** SUCCESS

## Changes
- **Schemas:**
    - `db/schemas/message.py`: Removed `model` column.
    - `db/schemas/conversation.py`: Added `model` column (String 100, nullable=False, server_default="kimi-k2.6:cloud").
- **Migration:**
    - Created `alembic/versions/3d8803d16ffc_move_model_to_conversation.py`.
    - Includes **data backfill** logic:
        - `upgrade`: Populates `conversations.model` from the most recent message in that conversation that has a model set.
        - `downgrade`: Restores `messages.model` from the parent conversation.
    - Includes **catch-up** logic:
        - Applies standard naming conventions to constraints (e.g., `uq_users_username`).
        - Adds missing SQL comments for all columns across all tables to meet project standards.
- **Verification:**
    - Ran `alembic upgrade head` → SUCCESS.
    - Ran `alembic downgrade -1` → SUCCESS (data restoration verified).
    - Ran `alembic upgrade head` → SUCCESS.
- **Documentation:**
    - Updated `CHANGELOG.md` with BREAKING CHANGE notice for Backend Agent.

## Backend Impact Notice
**BREAKING CHANGE:** The `Message.model` attribute is gone. Backend code must now use `Conversation.model`. The data has been migrated so no existing sessions are lost.
