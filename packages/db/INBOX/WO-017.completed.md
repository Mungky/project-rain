# WO-017: Completed
**Summary:** Set up Qdrant and MinIO datastores, merged configurations into the root orchestrator, and initialized storage buckets.

**Actions taken:**
- Added `qdrant` and `minio` to root `docker-compose.yml` and `db/docker-compose.snippet.yaml`.
- Created `db/qdrant_collections.yaml` with the `documents` collection (768 dimensions, int8 quantization).
- Created and executed `db/seeds/seed_minio_buckets.py` to initialize S3 buckets.
- Created root `.env.example` and synchronized it across all components.
- Verified service health (Qdrant & MinIO are healthy and reachable).

**Next Steps:**
- DB Agent can proceed to WO-018 (Document and Skill schemas).
