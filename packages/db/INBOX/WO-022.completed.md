# WO-022: Completed
**Summary:** Updated the `messages` table schema to support user feedback and model reasoning.

**Actions taken:**
- Modified `db/schemas/message.py` to include `feedback` (Integer) and `reasoning_content` (Text) columns.
- Generated and applied Alembic migration `3c737a5a064e_add_feedback_and_reasoning_to_messages`.
- Verified columns are correctly added with `TIMESTAMP WITH TIME ZONE` constraints via base mixins.
- Verified database is at migration head `3c737a5a064e`.

**Next Steps:**
- Backend Agent should implement feedback endpoints and reasoning parsing logic in WO-023.
