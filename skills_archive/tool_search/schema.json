{
  "name": "tool_search",
  "description": "Search for available tools, MCP connectors (Google Drive, Atlassian Rovo), and specific format skills (docx, pptx, xlsx, pdf, frontend-design) before creating complex files or using browser automation.",
  "version": "1.0.0",
  "parameters": {
      "type": "object",
      "properties": {"query": {"type": "string", "description": "Tool or skill to search for"}},
      "required": ["query"]
  }
}